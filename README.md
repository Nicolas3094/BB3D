For local changes, create .env file with the following structure:
```
.env

LOCAL_PATH=<LOCAL_PATH>

```
---

VSCode Set Up

After downloading C++ extensions. Under the .vscode folder:

Windows:

```
task.json
{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": <PATH_OF_BINARY_C++>,
            "args": [
                "-fdiagnostics-color=always",
                "-std=c++14",
                "-g",
                "src/*.cpp",
                "src/binPacking/*.cpp",
                "src/genetic/*.cpp",
                "src/heuristics/*.cpp",
                "src/metaheuristics/*.cpp",
                "src/readData/*.cpp",
                "src/spatial/*.cpp",
                "src/structures/*.cpp",
                "src/util/*.cpp",
                "-o",
                "main.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
```

```
launch.json

{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++ Runner: Debug Session",
      "type": "cppdbg",
      "request": "launch",
      "args": [],
      "stopAtEntry": false,
      "externalConsole": true,
      "cwd": <WORKSPACE_PATH>,
      "program": <WORKSPACE_PATH_PROGRAM>,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "C/C++: g++.exe build active file"
    }
  ]
}
```